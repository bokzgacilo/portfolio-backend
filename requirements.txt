# Pinned so a Render redeploy cannot pick up a breaking release on its own.
fastapi==0.128.8
uvicorn[standard]==0.39.0
python-multipart==0.0.20
pillow==11.3.0
pillow-heif==1.1.1

# rembg brings the ONNX model plumbing; onnxruntime is the CPU inference engine.
rembg==2.0.61
onnxruntime==1.19.2

# Office encryption and OOXML package editing for the Excel unlocker.
msoffcrypto-tool>=5.4,<6
lxml>=5.3,<6

# YouTube extraction for the video/mp3 downloader. Unpinned minor/patch: YouTube
# changes its player frequently and yt-dlp ships frequent fixes for it.
yt-dlp>=2025.10.14
